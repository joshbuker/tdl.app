div style="display: block; text-align:center;"
  = form_with model: Task.new, url: fuck_you_path do |f|
    = f.text_field :title, placeholder: 'Your Task'
    = f.submit 'Search'
  - if @tasks.present?
    ul
      - @tasks.each do |task|
        li = task.title
  - else
    h1 No Tasks found bro
